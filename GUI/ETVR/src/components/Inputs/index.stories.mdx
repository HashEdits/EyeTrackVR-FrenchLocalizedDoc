import { Canvas, Meta, Story } from '@storybook/addon-docs'
import Input from './index'

<Meta title="Inputs/Inputs" component={Input} />

export const Template = (args) => <Input {...args} />

<Canvas>
  <Story
    name="default input"
    args={{
      type: 'string',
      value: 'text',
      id: '1',
      setValue: () => {},
    }}
    argTypes={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
