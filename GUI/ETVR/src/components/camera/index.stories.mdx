import { Canvas, Meta, Story } from '@storybook/addon-docs'

import CameraContainer from './index'

<Meta title="Camera/Camera Container" component={CameraContainer} />

export const Template = (args) => <CameraContainer {...args} />

# Camera Container

<Canvas>
  <Story
    name="Camera Object"
    args={{
      activeStatus: 'active',
    }}>
    {Template.bind({})}
  </Story>
</Canvas>

## Description

## Props
