//import { useEffect } from 'react'

/* const startUpdater = () => {
  showNotfication({
    title: t('Installing Update v{{ v }}', { v: newVersion }),
    message: t('Please wait... Will restart automatically when done.'),
    autoClose: false,
  })
  installUpdate().then(relaunch)
}

const Updater = () => {
  const { update } = useUpdater()
  const { isUpdateAvailable } = useUpdateAvailable()

  useEffect(() => {
    if (isUpdateAvailable) {
      update()
    }
  }, [isUpdateAvailable, update])

  return null
}

export default Updater */
